import React from 'react';
import { Link } from 'react-router-dom';
import PropTypes from 'prop-types';

const ShowSingleDocument = ({title, content, access, id}) => (
  <div className="col s3 m3">
    
  </div>
);


export default ShowSingleDocument;
